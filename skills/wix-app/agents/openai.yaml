interface:
  display_name: "Wix App Builder"
  short_description: "Build Wix extensions with safe dashboard routing."
  default_prompt: "Use $wix-app to plan and implement a Wix app extension safely."
